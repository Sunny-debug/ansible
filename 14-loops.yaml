- name: Using loops to install various Services
  hosts: local
  connection: local
  tasks:
    - name: Install Services
      ansible.builtin.package:
        name: "{{ item }}" # Item is reserved
        state: latest
      loop: 
        - nginx
        - mysql
        - zip  
